# Python
__pycache__/
*.py[cod]
.venv/

# Editor (nvim, vscode)
.*.sw[a-p]
.vscode/

# QField-Exportpakete
qfield/

# QGIS
*.qgz~
*.qgs~

# Generierte GIS-Bündel (aus GeoJSON via 'reiseplan-cli build-gpkg')
*.gpkg

# Lokale Arbeits-/Scratch-Dateien
tmp/

# Generierte Webseite (aus GeoJSON via 'tools/build_site.py'; in CI nach Pages)
site/

# QField-Exporte gehören nach qfield/ (oben komplett ignoriert), nie nach qgis/.
# So bleibt qgis/ ein reiner Quell-Ordner (reiseplan.qgz, styles/, …) und ein
# versehentlich dort gelandeter Export fällt im 'git status' sofort auf.

# OSM-Roh-Cache (regenerierbar via tools/fetch_cfr_data.py)
data/raw/osm_ro_stations.json
